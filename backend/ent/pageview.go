// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"sub2api-extension/ent/pageview"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// PageView is the model entity for the PageView schema.
type PageView struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 页面 id, 来自 page-registry
	PageID string `json:"page_id,omitempty"`
	// 匿名访客 id
	VisitorID string `json:"visitor_id,omitempty"`
	// 是否管理员访问
	IsAdmin bool `json:"is_admin,omitempty"`
	// 记录创建时间
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PageView) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case pageview.FieldIsAdmin:
			values[i] = new(sql.NullBool)
		case pageview.FieldID:
			values[i] = new(sql.NullInt64)
		case pageview.FieldPageID, pageview.FieldVisitorID:
			values[i] = new(sql.NullString)
		case pageview.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PageView fields.
func (_m *PageView) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case pageview.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case pageview.FieldPageID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field page_id", values[i])
			} else if value.Valid {
				_m.PageID = value.String
			}
		case pageview.FieldVisitorID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field visitor_id", values[i])
			} else if value.Valid {
				_m.VisitorID = value.String
			}
		case pageview.FieldIsAdmin:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_admin", values[i])
			} else if value.Valid {
				_m.IsAdmin = value.Bool
			}
		case pageview.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PageView.
// This includes values selected through modifiers, order, etc.
func (_m *PageView) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this PageView.
// Note that you need to call PageView.Unwrap() before calling this method if this PageView
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *PageView) Update() *PageViewUpdateOne {
	return NewPageViewClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the PageView entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *PageView) Unwrap() *PageView {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: PageView is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *PageView) String() string {
	var builder strings.Builder
	builder.WriteString("PageView(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("page_id=")
	builder.WriteString(_m.PageID)
	builder.WriteString(", ")
	builder.WriteString("visitor_id=")
	builder.WriteString(_m.VisitorID)
	builder.WriteString(", ")
	builder.WriteString("is_admin=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsAdmin))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PageViews is a parsable slice of PageView.
type PageViews []*PageView
