# Changelog

## [未发布]

### 变更

- 管理端工单管理与用户端工单中心改为定高双栏工作台：列表与对话各自内部滚动，筛选、分页、状态切换和回复框固定可见，不再随长对话被推到页面底部。
- 工单状态改用统一的彩色徽章（待处理暖橙、处理中靛蓝、已关闭中性），列表行展示相对更新时间并标出当前选中项。
- 工单对话消息改为带头像的气泡，按发送方左右分栏；列表加载使用骨架屏，空数据和未选中状态补充图标与说明。
- 工单列表刷新按钮补充 Tooltip；工单管理页错误提示改用统一 `Alert`，未配置 Sub2API 集成时单独提示。
- 切换工单或收到新回复后对话区自动停在最新一条。
- 新工单提醒配置的加载、错误和已停用渠道改用统一的 `Alert`、`Badge` 与加载指示，勾选的渠道高亮显示。

### 修复

- 用户端工单中心未挂载全局通知容器，提交工单、发送回复的成功与失败提示不可见；现已随页面挂载右上角 Sonner 通知。
- 工单中心页面根元素引用了不存在的 `--aux-page-text` 变量，改为公开页统一的 `--aux-page-ink`。

## [0.10.0] - 2026-09-24

### 新增

- 促销活动用户端提前展示未来 3 天内开始的已上架活动，活动开始前可查看说明和返利规则，开始后再加载订单参与领取。
- 促销活动用户端在活动列表和详情中统一展示活动时间范围。
- 促销活动新增单个用户福利金额上限配置，按用户和活动累计已发放返利限制领取，达到上限后用户端提示“返利金额达到上限”；填写 0 表示不限。
- 促销活动超出用户剩余福利额度时改为发放剩余额度；多订单领取按返利金额从小到大优先消耗额度，支持单笔订单部分返利。
- Codex、Claude Code、Grok Build 等客户端页内新增「CC Switch（推荐）／手动配置」切换，默认显示 CC Switch；旧的 `?method=cc-switch&client=codex` 链接继续有效。
- 客户端接入范围补齐 Codex Desktop、Claude Code/Desktop、Grok Build、Gemini CLI、VS Code/Cursor 的 Codex 与 Claude 扩展、OpenAI 兼容客户端和 IDE 插件；配置地址、文件路径、重启与排错步骤均按本站网关重写。
- Claude Desktop 在 Claude 页内展示独立的 CC Switch 流程，说明模型映射与本地路由要求。
- 桌面端接入目录收敛为 Codex、Claude、Grok、Gemini、VS Code/Cursor 与其他客户端六个主题；具体客户端在页内切换，不使用折叠目录。
- Codex Desktop 页面移除独立的准备与安装模块，将地址和模型输入并入配置模块；CC Switch 配置区提供 CC Switch 与 Codex Desktop 官方下载入口，并移除外部配置说明链接。
- 新增 Sub2API 用户工单中心和管理端工单工作台，支持工单对话、状态更新，并可通过 `ticket.created` 事件配置新工单提醒渠道。
- 新增工单用户端上架开关，默认关闭；开启后同步发布 Sub2API 自定义菜单，关闭或服务重启时按持久化状态移除/恢复菜单。
- 工单问题描述、用户追加回复和管理员回复支持 Markdown 原文保存及安全展示，禁用原始 HTML 和危险链接。

### 变更

- 用户领取促销返利成功后，除右上角到账通知外，额外展示可关闭的到账金额弹窗。
- 促销活动用户端在活动列表和详情中展示单用户返利金额上限，无上限时显示“不限”。
- 新增客户端指南及 Codex 导入流程以随网关地址和模型更新的自绘操作示意代替截图；部分既有指南保留截图和放大查看交互。
- Codex 指南聚焦 Desktop，新手无需安装 CLI、Node.js 或 npm；手动 Tab 保留 `config.toml` 和 `auth.json`。Claude Code 手动 Tab 提供 `~/.claude/settings.json` 示例。
- Codex 的 CC Switch 推荐流程重构为五步引导；前四步提供带①—④操作定位标记的 Sub2API 用户端/CC Switch HTML/CSS 界面草图，CC Switch 参照官网首页产品区重绘，第五步「重启 Codex 客户端」仅保留文字说明。Sub2API 草图只展示用户可见侧栏，不包含管理板块；手动配置保留为备选。
- CC Switch 下载入口统一指向官方站点 `https://ccswitch.io/`。
- 新工单提醒配置移至系统消息通知日志上方，与发票申请通知并列。

### 修复

- 客户端接入文档和新手教程默认使用当前访问站点的域名生成 API 地址；Codex 的 `base_url` 与 CC Switch 接口地址保持为网关根地址，不再附加 `/v1`。

### 兼容性与升级

- 新增 `support_tickets` 与 `support_ticket_messages` 表，升级时由 Ent 自动迁移创建；工单用户端上架依赖现有 Sub2API 数据库集成与浏览器可访问的公开地址，默认关闭。若禁用自动迁移，升级前应执行迁移；升级前备份附属系统数据库及文件卷，二进制回退不会撤销新增表。无需新增环境变量。
- 促销活动新增 `promotions.max_rebate_amount` 字段，升级时由 Ent 自动迁移创建，旧活动默认值为 0（不限）；无需新增环境变量。

## [0.9.5] - 2026-09-21

### 修复

- 修复 ToB 官网顶部导航删除「全球网络」后，保存或重新读取配置时被自动补回的问题；修改其链接及清空全部导航也会按管理员配置保留。

### 兼容性与升级

- 无数据库结构、接口字段或环境变量变化，无需迁移；首次初始化和旧配置缺少导航字段时继续提供默认导航。
- 更新后，如旧版本已将「全球网络」补回并保存，请重新删除该项并保存，刷新管理页和公开官网确认生效；可回退至 `v0.9.4`，但回退会恢复自动补回行为。

### 已知限制

- 删除「全球网络」导航项只移除菜单入口，页面中的全球网络地图继续展示。

## [0.9.4] - 2026-09-21

### 变更

- 移除 ToB 官网及嵌入入口的「从方案确认，到稳定上线」交付流程模块，同时隐藏对应导航入口和配置开关。
- 页面及管理布局改为按路由加载，官网首访不再下载后台、API 文档和完整菜单图标库；入口 JS 的 gzip 体积由约 534 KB 降至 74 KB，ToB 官网全部初始 JS 约 177 KB。
- 为分包加载失败提供重新加载入口，管理端切换页面时保留侧边栏；构建增加入口及官网静态依赖的 gzip 体积预算检查。

### 修复

- 修复官网配置加载前展示默认文案的问题；ToC 和 ToB 官网在配置成功返回前保持无文案空壳，配置请求失败时也不回退展示默认官网内容。
- 修复官网加载时的阶段性闪变与卡顿：分包和配置等待使用相同背景，导航、文字及粒子球同时短淡入；全球网络底图改为预生成的本地 SVG，移除首访期间的地理网格计算和近 4,000 个 SVG 节点挂载。

### 兼容性与升级

- 仅调整前端展示与加载方式，无破坏性接口变更、数据库迁移或新增环境变量；既有官网配置和资源继续使用。
- 更新至本版本后，检查 ToC/ToB 官网及嵌入入口、粒子球和地图、管理端登录与页面切换；分包下载失败时可点击「重新加载」恢复。测量方法和验证结果见 [前端加载性能](docs/FRONTEND_PERFORMANCE.md)。
- 可回退至 `v0.9.3`，回退不会修改数据库或资源文件，但会恢复旧版整包加载、首屏动画和 ToB 交付流程模块。

### 已知限制

- 性能数据来自本地受控浏览器测试，线上收益取决于实际网络、缓存与设备；管理端完整菜单图标库仍为较大分包，官网首访不加载该分包。

## [0.9.3] - 2026-09-20

### 新增

- API 文档新增异步视频生成、任务状态查询和 MP4 内容下载端点，覆盖文生视频、图生视频、状态枚举、响应字段、`Range` 分段下载及可复制的多语言示例。

### 兼容性与升级

- 本次仅调整前端 API 文档和测试，Sub2API 接口行为、数据库结构、环境变量和部署方式均无变化，无需迁移。
- 升级后检查视频创建、轮询与下载示例，并确认同步图片编辑及异步图片任务文档仍然可见；可安全回退至 `v0.9.2`，回退不会修改数据，但会移除视频接口文档。

## [0.9.2] - 2026-09-20

### 修复

- 恢复 API 文档中的同步图片编辑端点 `POST /v1/images/edits`，补充 JSON 图片 URL、`multipart/form-data` 文件字段、请求参数、响应字段和可复制示例。

### 兼容性与升级

- 本次仅调整前端 API 文档和测试，Sub2API 接口行为、数据库结构、环境变量和部署方式均无变化，无需迁移。
- 升级后检查 API 文档中的同步编辑端点，以及异步生图提交和任务轮询示例；可安全回退至 `v0.9.1`，回退不会修改数据，但会恢复同步编辑文档缺失状态。

## [0.9.1] - 2026-09-19

### 修复

- 修复 ToB 官网菜单测试在 GitHub Actions 中未等待异步配置加载完成就断言的竞态问题，避免 Release 质量门禁偶发失败。

### 兼容性与升级

- 仅调整测试同步方式，运行时代码、数据库结构、接口和环境变量均无变化，无需迁移。
- `v0.9.0` 标签的 Release 因质量门禁失败而未生成；`v0.9.1` 包含 `v0.9.0` 的全部功能和发布产物，升级检查项沿用 `v0.9.0` 说明。
- 可回退至 `v0.8.2`；回退不会修改数据，但会恢复旧版官网与文档导航行为。

## [0.9.0] - 2026-09-19

### 新增

- 系统配置新增 `systemPosition`（`toc`/`tob`）开关；API 文档和客户端接入文档的官网入口会按定位跳转对应官网，无需数据库迁移。
- API 文档补充异步图片生成、异步图片编辑和任务状态查询端点，包含请求参数、响应契约、多语言示例与 Markdown 复制内容。

### 变更

- 系统配置页移除右侧示例卡片，保留系统名称、Logo 上传、系统域名、默认模型、系统定位和发布开关等实际配置项。
- 本地 Vite 默认挂载到 `/aux/`，开发代理会移除该前缀后转发 API、健康检查与客户端资源；开发环境公开地址示例同步改为包含 `/aux`。
- 管理控制台侧边栏移除「Sub2API 官网」快捷入口，官网公开路由和配置页预览入口保持可用。

### 修复

- 数据库动态官网现在会读取系统 Logo、文档按钮文字与链接、控制台链接和顶部导航配置；更新配置后无需重新生成页面内容。
- ToB 官网顶部菜单改为按 `navigationItems` 配置顺序和文案渲染，不再被硬编码菜单覆盖；旧配置缺少菜单时使用企业版默认导航。
- API 文档和客户端接入文档中的官网、跨文档和控制台按钮统一使用当前浏览器域名下的 URI，并在 iframe 场景从顶层窗口打开。

### 兼容性与升级

- `systemPosition` 保存在现有 `homepage.config` JSON 中，旧配置会自动使用 `toc`；无数据库结构迁移，也没有新增必填环境变量。
- 升级后检查系统配置 Logo、ToC/ToB 定位、动态官网文档按钮和顶部菜单，并分别从 API 文档与客户端接入文档验证官网、控制台和跨文档入口。
- 可回退至 `v0.8.2`；回退不会修改数据，但会忽略 `systemPosition`，并恢复旧版动态官网与 ToB 菜单渲染行为。

### 已知限制

- 文档页控制台按钮会保留配置 URL 的路径、查询参数和锚点，但使用当前浏览器域名；需要跳转到另一域名时应从官网或外部导航入口访问。

## [0.8.2] - 2026-09-19

### 修复

- 修复 `/aux/` 子路径部署时管理员配置的站内链接被再次添加 `/aux` 前缀的问题。`/login`、`/dashboard`、`/admin/users` 等根路径现在按同域名原样跳转到 Sub2API；进入 aux-system 时需显式配置 `/aux/...`。
- 首页主按钮、控制台、顶部导航、资源协议、合作伙伴和接入生态链接，以及 API 文档、客户端文档和动态 HTML 页面导航统一使用上述规则；页内锚点与完整 HTTP/HTTPS 地址保持原有语义。
- 动态 HTML 页面在 iframe 内点击同域 Sub2API 路径时改为顶层导航，避免目标页面被困在 aux 沙箱中；显式 `/aux/...` 路径仍可留在 aux 应用内。
- 官网配置页和集成文档补充链接填写说明，明确 Sub2API 与 aux-system 路径边界。

### 兼容性与升级

- 无数据库结构、接口字段、环境变量或持久化数据变化，无需迁移；API、图片、下载、登录重定向等 aux-system 自有地址仍自动使用 `/aux` 前缀。
- 升级后分别检查配置为 `/dashboard`、`/login` 和 `/aux/login` 的按钮，并确认前两者进入同域 Sub2API、后者进入 aux-system。
- 可回退至 `v0.8.1`；回退不会修改数据，但管理员配置的根路径链接会再次被重写到 `/aux/...`。

### 已知限制

- 同域部署依赖 NGINX 将 `/aux/` 转发至 aux-system，并将其他根路径保留给 Sub2API；独立域名部署若要跳转到另一域名，仍需填写完整 HTTP/HTTPS URL。

## [0.8.1] - 2026-09-19

### 修复

- 前端支持通过 `VITE_BASE_PATH` 部署到非根路径，生产镜像默认使用 `/aux/`；Vite 静态资源、React Router、管理端登录跳转、健康检查、API、埋点、下载和同源图片地址统一带上应用基础路径。
- 动态页面导航、官网、API 文档、客户端文档、文件管理和系统配置中的站内链接与资源地址统一适配 `/aux/`，避免请求误落到同域名下的 Sub2API 服务。

### 兼容性与升级

- 无数据库结构、接口字段或持久化数据变化，无需迁移；Nginx 继续将 `/aux/` 前缀移除后转发到 aux-system 的 `127.0.0.1:8004`。
- 发布镜像默认面向 `/aux/` 子路径。需要继续部署在独立域名根路径的自构建环境，应传入 `--build-arg VITE_BASE_PATH=/`。
- 升级后检查 `/aux/`、`/aux/assets/*`、`/aux/api/aux/*` 和 `/aux/admin/*`，并确认同域名下 Sub2API 的 `/` 与 `/assets/*` 保持正常。
- 可回退至 `v0.8.0`；回退不会修改数据库或上传资源，但 `/aux/` 页面会恢复为根路径资源和接口地址，无法在共享域名子路径下完整运行。

### 已知限制

- 基础路径在前端构建时确定；同一个镜像不能同时以 `/` 和 `/aux/` 两种路径提供前端页面。

## [0.8.0] - 2026-09-18

### 变更

- 项目展示名、Go module、健康检查服务名、JWT 新签发 issuer、前端包名、GHCR 镜像和二进制更新包统一由 `sub2api-extension` 改为 `aux-system`。
- 移除公开页面中的 `TERALEMO` 硬编码；API 文档、客户端文档、动态官网、发票和促销页面统一读取公开系统配置的 `siteName`，并兼容旧配置中的 `heroTitle`。
- 系统配置页支持选择或拖拽上传 Sub2API 系统 Logo，提供上传状态、预览、替换和移除操作；保存后官网与 API 文档统一读取 `siteLogoUrl`。
- 仓库内项目 skill 统一改名为 `aux-system-page-writer`、`aux-system-integration` 和 `aux-system-operations`。
- 复制现有 Sub2API 完整官网作为独立 ToB 官网 `/tob-home` 与 `/embed-tob`，保留原官网的 Hero、指标、快速接入、生态、安全、开发者、页脚和配置项，并新增 React Simple Maps 全球网络板块；管理端 `/admin/tob-homepage` 在完整官网配置基础上支持多个主服务器、CDN 集群和客户位置。
- ToB 全球网络配置新增节点名称开关、连线曲度和 `10%–200%` 节点大小滑块、三类节点颜色、实线/虚线以及数据流动效果开关；旧配置自动使用兼容默认值。
- ToB 官网调整为企业能力、全球网络、交付流程、生态与安全的阅读顺序，补充全球主服务器与 CDN 覆盖、中国大陆优化节点和企业交付说明；ToC 官网配置入口明确标记为「ToC 官网配置」。
- 全局操作结果通知统一使用右上角 Sonner toast，并在 `AGENTS.md` 与 `CLAUDE.md` 中补充 success、error、warning、info 的语义和使用边界。

### 修复

- ToB 官网配置保存时忽略尚未填写的新增节点行；已填写一部分的节点会提示具体类型、序号和无效字段。配置加载、校验与保存失败统一使用右上角 `error` 通知，不再在页面顶部显示错误条。
- ToB 全球网络地图改用完整覆盖全球经纬范围的高密度小粒子投影，美洲保持显示在右侧且不展示南极洲；地图横向铺满页面，移除节点统计条、图例、外框、独立底色和节点白色外圈，并缓存点阵计算以改善加载速度。
- ToB 官网优化标题行高、板块留白、移动端导航和响应式排版，统一「连接更快、更稳。」等强调文案的主题渐变效果。

### 兼容性与升级

- `/api/aux/*`、`SUB2API_EXTENSION_*`、Compose project/数据卷、服务名 `aux-backend` 与 GitHub 仓库 `FluxCode666/sub2api-extension` 保持兼容，无数据库迁移。
- ToB 配置继续独立保存到现有 `system_meta`；首次尚无 `homepage.tob.config` 时以当前 `homepage.config` 作为完整副本，不修改原官网配置。
- 新 Release 以 `aux-system_linux_*` 为主更新包，并在改名过渡期同时发布 `sub2api-extension_linux_*` 兼容别名；清单保留旧镜像坐标用于旧更新器校验，并单独记录实际 `aux-system` 镜像，现有实例可继续使用管理员更新完成首次升级。
- 容器部署升级时将 `SUB2API_EXTENSION_IMAGE` 改为 `ghcr.io/fluxcode666/aux-system`；不要更换已有 Compose project 或数据卷。回滚旧二进制不会撤销配置，旧 JWT 仍可按现有验签规则使用。

### 已知限制

- GitHub 仓库尚未执行外部重命名，因此源码标签与更新检查仍指向旧仓库地址；待仓库实际迁移后再单独调整该坐标。
- ToB 地图底图由浏览器从 jsDelivr 加载 world-atlas 数据；离线部署需要将该底图资源纳入同源静态资源并调整 `TobNetworkMap` 的 `GEO_URL`。

## [0.7.11] - 2026-09-16

### 修复

- OAuth 合并计费只需组内一个账号明确设置采购成本；未设置的成员继承该组成本，全组未设置时才使用全局默认值。多个明确成本不一致时仍拒绝保存，明确填写的零金额也参与校验。
- 消费核算从完整组配置读取采购成本，即使设置成本的成员在查询区间没有调用，也不会错误使用其他成员的默认值；同组采购成本仍只计一次。
- 单账号保存与合并计费失败使用与成功反馈相同的右上角通知组件，以 error 类型显示中文成本冲突提示，保留编辑内容以便重试。

### 兼容性与升级

- 本次为问题修复，无独立新增功能、破坏性接口变化、数据库结构或环境变量变化，无需迁移。
- 更新至 `v0.7.11` 后检查 `/health`，刷新成本配置与消费核算页面，确认只设置一个 OAuth 成本时可以合并，成本冲突时显示右上角错误通知。
- 配置仍保留各账号的原始成本和空值，不批量回填。可回退至 `v0.7.10`，但会恢复按有效成本比较的限制，只有部分成员填写成本的组可能重新报错或按默认值核算。

### 已知限制

- 同组多个账号明确设置成本时仍须保持金额一致，修改共享采购价时需同步调整这些明确值，或清空其他成员的成本以继承组内唯一金额。

## [0.7.10] - 2026-09-15

### 修复

- 成本配置的合并计费账号下拉改为每页加载 10 个匹配账号，滚动到底部自动追加下一页；搜索仍覆盖全部账号并保留跨搜索多选，避免大量账号同时挂载造成卡顿。
- 修复合并计费可选账号误显示禁止选择光标的问题，禁用样式只匹配真正禁用的选项。
- 修复合并账号下拉的滚轮被外层弹窗拦截的问题，使用 Radix 模态 Popover 保留列表滚动和关闭后的焦点恢复。
- OAuth 回本分析在未填写账号过期时间时，自动读取 Sub2API 已保存的订阅到期时间；影子账号可使用母账号订阅日期，仍优先采用管理员手填期限。
- 未获取到有效期限时显示“未获取到”，不再将未知状态标为“永不过期”。只提取订阅日期，不读取完整凭据或使用 token 的过期时间；非法日期不阻断账号同步。

### 兼容性与升级

- 账号下拉修复仅涉及前端，更新后刷新成本配置页即可；回退旧版本会恢复全量渲染和光标误判，已保存的合并计费配置不受影响。
- 无数据库结构、接口字段或环境变量变化，无需额外迁移；兼容缺少相关字段的旧版 Sub2API。
- 升级后等待启动/定时同步（默认 5 分钟），或在成本配置页点击“立即同步倍率”，再刷新消费核算。回退后重新同步将恢复仅使用手填期限的行为，已有成本与合并计费配置保持兼容。

### 已知限制

- 成本配置接口仍返回完整账号列表；下拉按 10 条分页加载，匹配账号较多时需要继续滚动或搜索缩小范围。
- 自动日期依赖 Sub2API 已成功获取并保存订阅信息；附属系统不会自行向账号供应商发起查询，消费核算使用最近成功同步的快照。

## [0.7.9] - 2026-09-14

### 修复

- 修复 `v0.7.8` 打开或刷新成本配置页时因全量逐账号同步、等待同步锁而触发 15 秒超时的问题。页面现在只读获取 Sub2API 账号列表并合并已有成本，新建且无使用记录的账号仍会立即展示。
- 保存独立成本或合并计费组时仅同步所选账号，处理与后台同步同时首次建档的冲突；保留原有 OAuth 采购价、API 手工倍率及计费组，保存后未建档账号仍保持可见。
- 请求超时显示“请求超时，请稍后重试”，保留调用方主动取消行为；首次加载失败提供重新加载入口，不再把默认费用与零账号展示为实际数据。

### 兼容性与升级

- 无新增功能、破坏性接口变化、数据库结构或环境变量变化，无需额外迁移；API 响应字段保持兼容。
- 更新至 `v0.7.9` 后检查 `/health`，刷新成本配置页，验证未使用账号的成本保存及合并计费。已保存数据可供旧版本读取；回退 `v0.7.8` 会恢复本次修复的加载超时风险。

### 已知限制

- 页面仍读取完整账号列表并在前端分页，需要可用的 Sub2API 数据库连接。手工和后台全量同步仍逐账号持久化，大量账号或数据库缓慢时手工同步仍可能超时，但不再阻塞页面加载。
- 已在隔离 PostgreSQL、真实 HTTP 与浏览器中使用 1,500 个账号和每次数据库操作 6 毫秒延迟复现并验证；未在生产数据库执行压测。

## [0.7.8] - 2026-09-14

### 修复

- 「账号独立成本与合并计费」在打开或刷新页面时自动读取 Sub2API `accounts` 列表，无使用记录的新账号也能立即配置独立成本和合并计费，无需等待定时同步或手工同步。
- 刷新账号时保留已保存的 OAuth 采购价、API 手工倍率与计费组；页面读取、手工同步和定时同步在同一服务实例内串行执行，避免新账号重复建档冲突。

### 兼容性与升级

- 无数据库结构、响应字段或环境变量变化，无需额外迁移；更新后刷新成本配置页即可。未配置 Sub2API 数据库时仍可读取已有本地配置；配置了账号来源后，读取失败会显示错误，不能视作空账号列表。
- 更新后检查 `/health`，并在成本配置页确认新建且无使用记录的账号可以展示和保存配置；回退至 `v0.7.7` 可继续使用已保存的账号成本配置，账号列表恢复为手工或定时同步后更新。

### 已知限制

- 自动获取账号需要可用的 Sub2API 数据库连接；同步串行化仅覆盖同一服务实例，多实例部署仍需关注同步重叠。

## [0.7.7] - 2026-09-14

### 新增

- 「OAuth 回本分析」和「账号成本明细」默认按账号创建时间倒序排列，支持点击账号创建时间、收入、利润或请求数表头切换正序与倒序；每个表格独立维护单列排序，切换排序后回到第一页。
- 「OAuth 回本分析」增加请求数和利润列；OAuth 利润按 OAuth 收入减采购成本、再减该收入对应税额计算，不混入合并计费组中的 API 收入与成本。

### 兼容性与升级

- 排序与 OAuth 利润展示均在前端基于现有响应字段完成，无数据库结构、API 或配置变化；升级后刷新消费核算页即可使用，回退至 `v0.7.6` 不影响历史数据。

## [0.7.6] - 2026-09-13

### 改进

- 优化成本配置页筛选布局、表格密度、列宽与移动端适配，提升账号成本配置操作的可读性。

### 修复

- OAuth 单号成本未配置时，输入框显示“未配置”提示。
- 成本配置页所有数字输入框移除上下增减控件，保留数字输入和校验能力。

### 兼容性

- 无数据库结构、API 或配置变化。

## [0.7.5] - 2026-09-13

### 新增

- 「账号独立成本与合并计费」列表增加成本配置状态筛选，可区分已配置独立 OAuth 成本/API 手工倍率与使用默认配置的账号。

## [0.7.4] - 2026-09-13

### 变更

- 将 OAuth 回本分析中的“用户计费”统一改为“收入”，并同步调整汇总文案。

## [0.7.3] - 2026-09-13

### 修复

- 修复消费核算收入取数字段：识别 Sub2API `usage_logs.actual_cost` 作为用户实际扣费，恢复 API 收入、毛利、税额、税后利润及 OAuth 回本数据；账号成本仍使用 `total_cost` / `account_stats_cost` 与账号倍率计算。
- 修复上海日期选择器在 UTC 容器中发生日期偏移的问题；无时区日期按 `Asia/Shanghai` 解析，与每日分组口径一致。
- 修复混合计费组的 OAuth 回本统计误把 API 收入计入回本进度，增加 API/OAuth 分项收入字段。
- 移除账号成本明细中的倍率/核算口径列，避免展示已取消的统计口径。

### 兼容性

- 账号明细响应新增 `api_revenue` 与 `oauth_revenue` 字段，旧客户端可忽略；无需数据库迁移。

## [0.7.2] - 2026-09-13

### 修复

- 优化消费核算：当 Sub2API usage_logs 没有明确收费字段时，不再把实际成本冒充收入，也不再展示虚假的毛利、税额和利润率。
- 无收入字段时页面切换为 API 用量与成本监控，账号明细聚焦请求数、Token、API 成本、OAuth 一次性采购成本及账号倍率；每日明细仍保留 API 利润列，但以不可用占位显示。
- 每日明细增加 API / OAuth 账号类型切换，按日期汇总展示；OAuth Tab 仅展示当天利润（用户计费），另增 OAuth 回本分析板块展示账号级回本数据，不在每日明细中展示无法可靠核算的成本和回本进度列；API Tab 固定展示 API 收入，移除倍率/口径列，收入可用时展示毛利、税额、税后利润和利润率，并为四个利润指标增加公式提示。
- 账号成本明细新增账号类型和平台筛选，保留账号名/计费组搜索与创建时间范围筛选；表格明确展示收入、毛利、税额和利润列，无明确收费字段时以不可用占位显示。
- 日期筛选改为单入口范围日历，支持桌面双月、移动端单月和清除范围。
- OAuth 回本分析增加账号创建时间和过期时间；过期时间同时显示剩余天数，永不过期账号显示“永不过期”。
- 消费核算页移除 API 账号成本走势图和核算口径区域，保留消费概览、每日明细、OAuth 回本分析和账号成本明细；账号名称与账号 ID 分行展示并增加间距。

### 兼容性与升级

- 新增响应字段 `revenue_available`、`revenue_source`、`api_account_count` 和 `daily_accounts`，旧客户端可忽略。
- 附属库 `account_cost_configs` 新增可空列 `account_expires_at`，由同步读取 Sub2API `accounts.expires_at` 写入；设置 `AUTO_MIGRATE=false` 的部署需先执行 `make migrate`，旧客户端可忽略新增响应字段 `account_expires_at`。

## [v0.7.1] - 2026-09-12

### 修复

- 修复 Sub2API 菜单被手动删除或重复创建后，扩展系统入口无法可靠恢复的问题；统一重建管理员菜单项并补齐 iframe 所需的空 `page_slug`。
- 将管理员菜单名称固定为“扩展系统↗”，避免与可配置的站点名称混淆。

### 兼容性与升级

- 无数据库结构变化；升级后重新保存系统配置中的“上架到 Sub2API”开关即可重建菜单。

## [v0.7.0] - 2026-09-12

### 新增

- 系统配置页新增“上架到 Sub2API”管理员开关，可同步在 Sub2API 管理员菜单添加或移除 `/admin/dashboard` 入口。
- 新增促销活动管理：支持固定金额或支付金额百分比返利、活动时间窗、启停和 Sub2API 用户端菜单上架。
- 新增用户端促销活动页，用户可选择已完成充值订单领取返利；每笔订单全局唯一约束防止重复领取。
- 新增活动数据统计，展示参与用户、领取订单、支付金额和返利总额。

### 改进

- 促销返利领取现在会通过 Sub2API 数据库事务实际增加用户余额，并写入累计充值与审计记录；使用活动和订单幂等键防止重复入账，余额服务不可用时不会保存领取记录。
- 促销订单仅在活动时间范围内可参与，且同一订单全局只能领取一场活动的返利；后端会在订单列表和领取提交时分别校验时间与重复领取。
- 促销用户端页面的品牌名称改为读取系统配置中的网站名称，兼容旧版 `heroTitle` 配置。
- 促销用户端侧边栏新增已结束活动分组，默认折叠并按结束时间倒序展示；已结束活动不可领取返利。
- 促销用户端增加轻量 GSAP 进入和切换动效，并尊重 `prefers-reduced-motion`。
- 管理端促销活动列表改为渲染安全的 Markdown 活动说明，保持与用户端展示一致。
- 管理端促销活动列表新增活动名称模糊搜索和活动日期范围筛选。
- 管理端促销活动支持编辑活动名称、Markdown 说明、返利规则、时间范围和启用状态；停用活动时会同步下架。
- 促销返利入账后支持通过 `SUB2API_REDIS_*` 删除 Sub2API 余额缓存，并复用认证缓存失效队列，避免网关继续读取旧余额；未配置 Redis 时保留数据库正确性并明确最终一致性窗口。
- 促销用户端切换活动时保留当前说明与订单，等新订单返回后整体更新，避免列表闪烁和加载期间的布局跳动；重复点击当前活动不再重载，过期响应不会覆盖最新选择，失败后支持重试。
- 消费核算趋势图和每日明细改为 API 账号专属口径；OAuth 采购成本按账号一次性计入区间总账和账号明细，不再归集到首次使用日。
- 消费核算的“每日明细”按日期倒序展示，最新日期置顶；明细表统一使用 shadcn/ui Table。
- 成本配置列表按账号创建时间倒序排列，同一时间按 ID 倒序，未知时间置后；创建时间独占一列并显示到秒，账号 ID 紧跟账号名称。
- 默认仅展示未删除账号；按账号名称或 ID 搜索时包含匹配的已删除账号，并标记“已删除”。新增可搜索的平台下拉，支持与 OAuth/API 类型、创建日期范围和分页组合使用。
- 改造的账号表格、成本输入、计费组下拉与保存操作统一使用 shadcn/ui；筛选、分页仍保留未保存编辑，合并弹窗仍可选择历史账号。

### 兼容性、配置与迁移

- 新增附属系统 Ent 表 `promotions` 与 `promotion_claims`，生产启动时由现有幂等 `Ent Schema.Create` 自动创建；设置 `AUTO_MIGRATE=false` 的部署需要先执行 `make migrate`。
- `promotion_claims.payment_order_id` 改为全局唯一索引。已有重复订单记录需在升级前清理，使用 `AUTO_MIGRATE=false` 的部署需先执行 `make migrate`；新版本回退不会自动恢复旧的复合唯一约束。
- 返利领取需要配置 Sub2API PostgreSQL 连接；领取成功后同时写入扩展记录和 Sub2API 余额。Sub2API 数据库连接不可用时接口返回 503，不会创建虚假的领取记录。

- 附属库 `account_cost_configs` 新增可空列 `account_deleted_at`，只通过同步读取 Sub2API 的 `accounts.deleted_at` 写入，账号成本保存不能修改删除标记。Sub2API 表结构不变，历史计费配置不删除。
- 启动默认自动完成 Ent 迁移；若设置 `AUTO_MIGRATE=false`，先使用新版本执行 `make migrate`。升级后完成一次账号同步以回填删除状态，再核验默认列表与名称/ID 搜索；上游未提供 `deleted_at` 时按未删除处理。
- 新增可选 `SUB2API_REDIS_*` 环境变量；没有配置时不影响数据库入账，但网关余额缓存按原 TTL 最终一致。无破坏性 API 变化；回退旧版本可保留新增列，无需删列或回滚业务数据。

### 已知限制

- 列表筛选和分页沿用前端处理，首次请求仍读取完整成本配置；删除状态以最近一次成功同步为准，Sub2API 不可用时保留本地配置。

## [0.6.9] - 2026-09-12

### 新增

- 成本配置的「账号独立成本与合并计费」列表支持分页（默认 20 条，可选 10/20/50/100 条）、OAuth/API 类型和账号创建日期范围组合筛选；切换筛选或每页条数回到第一页，翻页和筛选保留未保存的账号编辑。
- 筛选及分页使用 shadcn/ui 的 Input、Select、Calendar、Popover、Pagination 与 Button；创建时间使用单个日期范围按钮和范围日历（桌面双月、移动端单月），支持同日、跨月、反向选择与清除。按浏览器本地时间筛选，包含结束当天，设置日期条件时排除创建时间未知的账号。
- 补齐客户端接入页功能埋点，覆盖章节导航、平台切换、安装下载、截图查看和前置指南跳转，保留已有客户端选择、复制与主题事件。
- API 文档页新增端点展开/收起、参数页签、示例语言、章节导航、跨文档跳转和成功复制统计，数据汇总至分析仪表盘。
- 新增文档页埋点说明，明确事件 ID、访问量口径与数据边界；事件请求体不采集用户填写的地址、模型、token 或复制正文。

### 修复

- 修复 API 文档兼容入口 `/docs` 未统计访问的问题，统一归入 `api-docs`，不重复增加仪表盘页面行。

### 兼容性、配置与迁移

- 复用现有埋点 API 和数据表，无破坏性变化、无配置或环境变量变化，无需数据库迁移；旧客户端选择、复制和主题事件保持兼容。
- 成本配置复用现有完整账号响应完成前端分页与组合筛选，无 API、数据库或配置变化。
- 源码构建默认版本同步至 `0.6.9`；发布镜像和更新包继续由 tag 注入版本号。

### 升级与回滚

- 使用控制台更新入口或更新至 `v0.6.9` 镜像，按现有部署方式重启后检查 `/health`，访问文档并在分析仪表盘核验新增计数，同时检查成本配置分页与日期筛选。
- 可按现有更新流程回退至 `v0.6.8`，已有埋点数据继续保留。

### 已知限制

- 成本配置首次加载仍读取完整账号列表，分页不会减少首次请求的数据量。
- 新增事件从升级后开始累计，无法补算历史未采集操作；访问统计为累计 PV，复制仅在成功时计数。
- 浏览器拦截、网络故障和匿名埋点限流可能导致少计；不影响阅读、导航和复制。

## [0.6.8] - 2026-09-11

### 新增

- GitHub Release 发布结果支持飞书机器人通知。
- GitHub Release 发布结果支持 SMTP 邮件通知，可配置多个收件人。
- 通知失败不会影响 Release 主流程。

## [0.6.7] - 2026-09-11

### 修复

- 移除图片上传的 10MB 限制，改为流式落盘，并显示后端返回的真实错误信息。
- 容器启动时自动修复资源挂载目录权限，解决符合格式的小图片也可能全部上传失败的问题。
- 修复官网开发者 CTA 入场动画在恢复深度滚动位置时可能偏离文案列的问题。

## [0.6.6] - 2026-09-11

### 新增

- 管理端每小时自动探测正式版本，有新版本时在当前版本号处显示更新提醒。

### 修复

- 官网配置支持保存文件管理页生成的站内 Logo 资源 URL，避免保存后被清空。

## [0.6.5] - 2026-09-11

### 改进

- 加宽官网关键指标区的首 Token 响应时间列，改善较长指标值的展示与可读性。

## [0.6.4] - 2026-09-10

### 修复

- 修复生产持久卷未灌入客户端资源种子时图标/截图 404 的问题：资源目录缺失文件时回退到内嵌种子，持久卷上的文件仍优先。

## [0.6.3] - 2026-09-10

### 改进

- 控制台更新完成后服务自动重启并加载新版本（对齐 sub2api：进程优雅退出，由 Docker `restart: unless-stopped` / systemd `Restart=always` 自动拉起），管理员无需登录服务器操作。
- 前端在更新成功后自动轮询健康检查，服务恢复后刷新页面显示新版本号。
- 客户端接入文档截图与客户端图标挪入系统统一资源目录（`assets.dir` 的 `client-docs/`、`client-icons/` 子目录），不再随前端构建产物打包，可在持久卷上独立更新。
- 补充生产 NGINX 反代配置示例（`deploy/nginx/conf.d/`），并移除 docker-compose 中未使用的数据卷声明。

### 修复

- 修复生产 embed 构建下 `/client-docs/` 仍输出资源目录列表而非 SPA 页面的问题（此前修复仅覆盖源码目录托管模式）。

## [0.6.2] - 2026-09-10

### 新增

- 启动时自动执行数据库迁移（Ent Schema.Create 幂等）
- 可通过环境变量 `AUTO_MIGRATE=false` 禁用自动迁移

### 改进

- 删除独立的 `aux-migrate` 容器，简化部署流程
- 更新成功后显示明确的重启指引和命令，包括具体的 `docker restart` 和 `docker compose restart` 命令
- 添加容器重建警告，提醒用户不要使用 `docker compose up -d` 以防止更新丢失

### 修复

- 修复更新后"刷新控制台"按钮误导用户的问题，改为明确的"我已重启，重新检查"

## [0.6.1] - 2026-09-10

### 修复

- 修复 `/client-docs` 路由返回目录列表而非 SPA 页面的问题
- 使用通配符路由处理 `/client-docs/*`，确保空路径或根路径返回 index.html
- 确保带查询参数（如 `?client=codex`）的访问能够正确显示客户端接入指南页面
- 修复相关单元测试，避免 Gin 精确路由与通配符路由冲突

## [0.6.0] - 2026-09-10

### 发布与更新

- 版本 tag 改为触发完整 CI、多架构 Docker 镜像构建和中文 GitHub Release 发布，移除生产 SSH 自动部署。
- 发布附带应用镜像、linux/amd64 与 linux/arm64 二进制更新包、校验文件和中文安装更新指南，正式版本与预发布版本分开处理。
- 管理员控制台左上角显示真实构建版本，点击可查看最新正式 Release、发布说明及更新入口。
- 更新改为主应用内置：下载当前平台二进制、校验后原子替换并保留 `.backup`，不再需要独立更新容器或 Docker socket。
- 现有安装无需启用额外服务；按 `deploy/UPDATES.md` 更新 Compose 与应用镜像后即可在控制台更新。二进制回退不会撤销数据库迁移。

## [0.5.0] - 2026-09-09

本次版本新增可配置官网、客户端接入指南和 API 接口文档，并补齐运营成本核算与统一文件管理。

### 新增功能

- 新增官网与后台配置入口，支持站点名称、系统域名、控制台链接、导航菜单、首页内容、合作伙伴及集成应用配置。
- 新增客户端接入文档，覆盖 Claude Code、Claude Desktop、Codex、Pi、Hermes、OpenClaw、Paseo、ZCode、DeepSeek Harness 和 Obsidian（Claudian）。
- 在 Claude Code、Claude Desktop、Codex、Pi 和 Hermes 各自的指南中提供 CC Switch 快捷配置，并补充配置截图、客户端图标和前置指南跳转。
- 新增 API 文档页，覆盖模型列表、Chat Completions、Responses、文本向量、图片生成、Anthropic Messages 和 Gemini Generate Content，提供请求及响应示例、代码复制和接口 Markdown 导出。
- 新增运营成本与利润统计，支持账号独立成本、API 成本倍率、OAuth 采购成本、税点配置，以及 API / OAuth 混合计费组归集。
- 新增统一文件管理，集中展示图片和发票文件，支持图片上传、链接复制、发票下载、原始文件名展示和文件备注维护。

### 体验优化

- 文档页支持浅色、深色与跟随系统主题，优化移动端布局、侧栏搜索、章节导航、代码高亮及接口分组展示。
- 客户端接入文档增加 GSAP 入口动效、配置切换反馈、截图放大和滚动回顶，并适配系统减少动效偏好。
- 文档页根据系统配置读取站点名称及版权信息，接入文档的 API 基础地址默认读取系统域名，控制台按钮复用官网配置链接。
- 明确 Codex 的 API Key 保存在 `~/.codex/auth.json`；更新 Claude Code 与 Codex 的默认模型示例，补充 npm 安装方式，以及 ZCode、Pi 的多协议说明。
- 统一使用发送「当前时间」的方式验证客户端接入，并引导用户核对控制台请求记录。

### 修复与维护

- 修复官网在嵌入页面中跳转控制台的行为，统一在当前顶层页面打开对应链接。
- 修复缺少 `matchMedia` 的运行环境中，滚动回顶动效初始化导致接入文档无法加载的问题；此时回退到直接滚动。
- 更新依赖以修复 fast-uri 高危安全公告，保持 pnpm 9 工作区兼容，并忽略本地设计检查与浏览器验证产物。

### 升级说明

- 本版本包含账号成本配置和文件备注相关数据库结构更新；生产发布流程会先执行数据库迁移，再启动新版本服务。手动部署时也需要执行迁移。
- 更新后可在系统配置与官网配置中设置站点名称、系统域名及控制台链接，供官网和文档页读取。

## [0.4.4] - 2026-09-03

### Fixed

- 修复通用消息通知模块的连接关闭错误未处理及错误提示格式问题，确保生产质量门禁可以通过并正常发布。

## [0.4.3] - 2026-09-03

### Changed

- 用户端开票申请改为每次仅支持一笔订单，申请记录支持滚动分页并区分线下支付记录。
- 管理端发票列表查询支持企业/税号模糊搜索、用户邮箱选择、日期范围和状态筛选，并统一使用查询分页交互。
- 管理端侧边栏收起为图标模式时，左上角品牌图标保持居中且完整显示。

### Fixed

- 修复未知路由意外跳转到用户首页的问题。
- 用户端发票页页脚品牌更新为 TERALEMO，下载发票按钮使用下载图标。

## [0.1.0] - 2026-08-20

### Added

- 增加 sub2api 附属管理端，根路径重定向到 `/admin/dashboard`。
- 增加数据库动态页面管理，支持 HTML、React/TSX、页面元数据、公开页和管理员页。
- 增加图片资源管理、动态菜单图标和动态页面管理员 API 工具。
- 增加页面访问量、功能点击埋点和分析仪表盘。
- 增加 `sub2api-extension-page-writer` 等项目协作 skill。

### Changed

- 将页面内容和配置统一落到附属系统自己的 PostgreSQL `pages` 表。
- 生产部署支持推送 semver tag 自动触发，并使用 tag 作为镜像版本号。

### Fixed

- 修复动态 HTML iframe 中非页内链接的顶层跳转，避免开发环境 `origin=null` 导致的空白页。
- 修复仪表盘布局、标题裁切和轨道节点动画视觉问题。
